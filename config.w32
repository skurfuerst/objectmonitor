// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("objectmonitor", "for objectmonitor support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("objectmonitor", "enable objectmonitor support", "no");

if (PHP_OBJECTMONITOR != "no") {
	EXTENSION("objectmonitor", "objectmonitor.c");
}

